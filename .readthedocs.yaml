version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3.8"
  commands:
    - pip install -r requirements.txt
    - pip install .
    - make html  # Or specify another build command if using Sphinx, MkDocs, etc.

python:
  install:
    - requirements: requirements.txt
